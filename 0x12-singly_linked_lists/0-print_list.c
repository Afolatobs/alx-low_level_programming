#include "lists.h"

/**
 * print_list - function
 * main - function
 * @h: list_t
 * @list_t: node
 * Return: 0
 */

size_t print_list(const list_t *h)
{
	if (str == NULL)
	{
	printf("[0] (nil)\n");
	}

	else
	{
		printf("the list_t is empty.\n");
	}

	return (list_t);
}

int main(void)
{
	list_t *h;
	list_t *dou;
	list_t *trois;

	h->len = NULL;
	h->next = dou;
	dou->len == NULL;
	dou->next == trois;
	trois->len == NULL;
	trois->next == NULL;

	return (0);
}
